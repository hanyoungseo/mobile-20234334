name: personnalitytest
description: "A personality test Flutter app"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.2.1
  firebase_database: ^12.1.0
  firebase_analytics: ^12.0.4
  firebase_remote_config: ^6.1.2

  # AdMob
  google_mobile_ads: ^5.1.0

  # 공유 기능
  share_plus: ^10.0.0

  # 즐겨찾기 / 로컬 저장
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
